export default class UserLoginDTO {
    constructor(first_name, email, role, id){
        this.first_name = first_name,
        this.email = email,
        this.role = role,
        this.id = id
    }
}